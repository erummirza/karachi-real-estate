# # MongoDB connection string. Use a local instance or a MongoDB Atlas URI.
MONGODB_URI="mongodb://localhost:27017/karachi_real_estate"

# # Required for AI-powered inventory parsing. If left as the placeholder value
# # below, the app automatically falls back to the client-side rule-based parser.
# # GEMINI_API_KEY="MY_GEMINI_API_KEY"

# # Port the Express server listens on.
PORT=5000

